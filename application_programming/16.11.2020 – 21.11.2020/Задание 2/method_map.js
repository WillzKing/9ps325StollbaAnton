let gach = ['Welcom','to', 'the', 'club', 'learherman'];
let much = gach.map(function(elem) {
	return elem + '!'
});
let i = 0;
let kekw = much.length;
for (i = 0; i < kekw; i++){
     alert(much[i]);
}
document.write(much);